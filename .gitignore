# Runtime / local secrets (deploy dir may be co-located)
DEPLOY.txt
certs/
certbot/
data/
generated/
nginx/nginx.conf
docker-compose.yml
# Only runtime site at repo root — keep templates/site tracked
/site/
!templates/site/
!templates/site/**
.DS_Store
*.pem
